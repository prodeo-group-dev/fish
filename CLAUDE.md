# FiSH

FiSH (Financial [Information] Systems Handler) is the shared General Ledger (GL) Engine platform underlying three related ventures: **Purse** (a Christian credit union — UK, Northern Ireland, Republic of Ireland, and a Sierra Leone agrifinance play), **Scrip** (Purse's treasury/investment arm, in Rust), and **Osusu** (rotating-savings/micro-lending run through Purse, in Kotlin). The GL Engine is also offered as a standalone B2B ledger/SaaS product to any company, with a go-to-market emphasis on the Mano River region (Sierra Leone, Liberia, Guinea, Côte d'Ivoire).

**BuzzMe is a separate Prodeo Group system, not a FiSH venture — do not conflate the two (direct instruction, 2026-09-19).** It has its own repo (`C:\Users\femif\Claude\Projects\BuzzMe`) and its own docs (`BuzzMe/docs/`), not this repo's. Its first vertical (Identity/DPID, school attendance via The Principal) has no relationship to FiSH at all; only its second, later vertical (Payment) is expected to eventually post through FiSH's GL Engine the way Scrip/Osusu already do — "separate but symbiotic," per the user's own architecture principle, not "underlain by FiSH" the way Purse/Scrip/Osusu are. Historical design work that predates this separation (2026-08-20 → 2026-09-19) still shows up in this repo's own git history — nothing further BuzzMe-related belongs here going forward.

## Read these first

- `FiSH_GL_Engine_Spec.docx` — requirements/spec (v0.7 as of 2026-08-11). Business context, scope, functional/non-functional requirements, data model, open questions.
- `docs/DDD_Design.md` — the DDD model: ubiquitous language, bounded contexts, aggregates, package structure, first TDD test specifications.
- `docs/SL/FiSH_GL_Business_Plan.md` — Sierra Leone business plan (the market-facing wedge). **`docs/SL/SL_Tax_And_Currency_Settings.md` + `.yaml` (2026-08-22)** extends the same UK/IE/NG pattern to Sierra Leone — but this one closes a previously-*open* gap rather than adding scope, since SL was already named first in the spec's own tax-computation scope list. Real, current NRA-derived figures: 30% Corporate Income Tax (raised from 25% this fiscal year, Finance Act 2026 — coincidentally matches `TaxRuleTest.kt`'s pre-existing illustrative `0.30` fixture, which predates this doc and wasn't sourced from anything), 15% flat GST (Sierra Leone's VAT-equivalent, single-rate unlike UK/IE/NG's multi-band VAT), NASSIT 5%/10% employee/employer. One genuinely unresolved gap, not just unsourced: PAYE's monthly band thresholds (structure confirmed: 5 bands, 0/15/20/25/30%) are reported inconsistently across sources as old-Leone (SLL) vs. New-Leone (SLE, post-2022 redenomination) — flagged `thresholdUnitConfirmed: false` in the YAML rather than picked silently; the one primary NRA PDF found failed to extract as readable text. **`docs/LR/LR_Tax_And_Currency_Settings.md` + `.yaml`** and **`docs/GN/GN_Tax_And_Currency_Settings.md` + `.yaml` (both 2026-08-22)** close out the four Mano River jurisdictions named in the spec's own tax-scope list. **Currency decision, 2026-08-22: this system only recognises `SLE` across the Mano River operation** — Liberia's LRD and Guinea's GNF are documented in both docs for reference only, explicitly marked `onboarded: false` in the YAML, not seeded into `Money`/`Company.baseCurrency` anywhere. Liberia: 25%/30% sector-split Corporate Income Tax (general/mining-oil), GST mid-transition (12%→13% enacted March 2026, full VAT replacement planned 2027 — treat as unstable), PAYE bands, and a genuinely **unresolved** NASSCORP conflict (two incompatible source versions, recorded as an open conflict rather than picked). Guinea: 35%/30% Corporate Income Tax (standard/mining), 18% flat TVA (genuinely VAT terminology, not GST like SL/Liberia), and the best-sourced income-tax band table in the whole document set (Article 63, Law L/2021/032/AN, six brackets 0%–20%) alongside an itemized 18% employer CNSS rate. **`docs/CI/CI_Tax_And_Currency_Settings.md` + `.yaml` (2026-08-22)** completes the four-jurisdiction Mano River tax-scope list (SL, LR, GN, CI all done). Currency: XOF (West African CFA franc, shared across all 8 UEMOA states — not Côte d'Ivoire-specific) is reference-only, `onboarded: false`, same SLE-only policy as LR/GN. BIC (Corporate Income Tax) is residency-split (25% resident/35% non-resident), plus a novel 2026 30% rate for non-established online marketplaces. TVA is genuinely volatile in one spot — a reduced 9% agricultural rate that changed three times (exempt → 18% → 9%) within two weeks in January 2026, flagged as unstable rather than settled policy. ITS reflects a real 2024 structural reform (Ordinance 2023-718/719) that abolished and merged three prior separate taxes (IS/IGR/CN) into one — any source describing "IGR bands" separately predates the reform and is superseded. CNPS is the most granularly itemized payroll levy in the whole document set (rate ranges by risk category, a separate CMU health line, two different contribution caps).
- `docs/UK/` — UK credit union business plans and the ethical/theological lending framework (source of the APR ceilings, arrears process, and prohibited-purpose rules referenced in the spec). **Convention, 2026-08-22**: the national folders under `docs/` (`UK/`, `IE/`, `NG/`, alongside `SL/`) are where country-specific settings — currency, tax law, and similar jurisdiction-bound rules — live, as each jurisdiction's version gets built out. `IE/`/`NG/` were placeholder folders (empty as of 2026-08-22, now populated the same day — see below); `SL/` already holds its business plan; `UK/` is the most populated today but predates this convention being made explicit, so its current contents (business plans, requirements docs) aren't a perfect fit for the convention yet — nothing has been reorganized to match it. **`docs/UK/UK_Tax_And_Currency_Settings.md` (2026-08-22)** is the convention's first real content: real, current HMRC-sourced Corporation Tax/VAT/PAYE/NI figures plus GBP currency settings — not the codebase's own fictional numbers, and not fabricated, per the explicit prior precedent against inventing UK tax figures (`HR/.../payroll_tax_rule.kt`'s KDoc). Corporation Tax maps to the already-built `TaxType.CORPORATE_INCOME_TAX` (though `TaxRule`'s flat-rate shape doesn't yet support the real small-profits/marginal-relief/main-rate tiering); VAT figures are recorded for a `TaxType` that doesn't exist yet; PAYE/NI figures are recorded for `HR/`'s still-unpopulated `PayrollTaxRule`. Reference data only — none of the consuming code changed. **`docs/UK/uk_tax_and_currency_settings.yaml`** is the same figures in structured key-value form (nested by section), kept in sync by hand — no code reads it yet either. **`docs/UK/NI_Jurisdiction_Specification.md` (2026-09-13)** answers, for Northern Ireland (one of Purse's four named home markets, previously folded silently into "UK"), whether it needs separate platform/integration work — confirmed research says almost none: same Companies House (NI-prefixed numbers only, e.g. `NI030452`), same HMRC/MTD, same UK Open Banking Standard, same GBP. The one genuine divergence is VAT-on-goods under the Windsor Framework (UKIMS "at risk"/"not at risk" goods test, "XI"-prefixed VAT numbers for EU-facing goods transactions) — currently moot platform-wide since no VAT `TaxType` exists yet, recorded for whenever VAT computation is eventually built. Also flags a dormant, never-commenced NI Corporation Tax devolution power (2015 Act) as a watch-item, not a current gap. **`docs/IE/IE_Tax_And_Currency_Settings.md` + `.yaml` (2026-08-22)** is the same pattern for the Republic of Ireland — real, current Revenue.ie-sourced figures, grounded in Purse's own stated ROI expansion and `docs/HR_Payroll_DDD_Design.md`'s "UK and Ireland" `PayrollTaxRule` scope. Corporation Tax here is trading-vs-passive (12.5%/25%), a genuinely different *shape* from the UK's small-profits tiering, not just different numbers — `TaxRule` can't represent either. One figure deliberately left blank rather than guessed: employer-side PRSI is banded, not sourced with confidence, and needs a follow-up lookup. **`docs/NG/NG_Tax_And_Currency_Settings.md` + `.yaml` (2026-08-22)** does the same for Nigeria, but with an explicit scope caveat up front — Nigeria isn't mentioned anywhere else in this project (Mano River is SL/Liberia/Guinea/Côte d'Ivoire only), so this was built on direct instruction despite that gap, not because Nigeria's in-scope status was independently confirmed. Reflects the Nigeria Tax Act 2025 (effective 2026-01-01) — a third distinct Corporation-Tax-equivalent shape (flat 30% CIT with a turnover-*and*-fixed-assets small-company exemption, plus a separate 4% Development Levy). Flags employer pension contributions as actively under PenCom review, not settled.
- `docs/Ecosystem_Extraction_DDD_Design.md` — design-only, 2026-08-20. Scopes extracting Purchase Order Processing, Sales Order Processing, and Inventory Management out of the GL Engine repo into API-only consumers, mirroring HR/Payroll's and Lending's prior extractions. Nothing extracted yet.
- `docs/Purchase_Order_Processing_DDD_Design.md` — design-only, 2026-08-20. The detailed follow-on to the Ecosystem doc's §1.1, grounded in `docs/UK/Purchase_Order_Processing_Requirements_Use_Cases.md` (UC-PO1-7/FR-PO01-08/NFR-PO01-05). Flags a real behavior change (AP recognition moves from PO-send to three-way-match) and several open questions (extraction destination, approval threshold, match tolerance) — not yet built in `POP/`. **`GL/`'s own legacy `PurchaseOrder` (the pre-extraction implementation, still the only live one) got a real Goods-in-Transit/GRNI increment 2026-08-21** — see the `IFRS_GL_Posting_Matrix.md` bullet below — worth reading before POP's own three-way-match design gets built, since the delivery-terms/timing logic will likely need porting rather than re-deriving.
- `SOP/docs/Sales_Order_Processing_Requirements_Use_Cases.md` — requirements/use-cases (UC-SO1-7/FR-SO01-09/NFR-SO01-05) for Sales Order Processing as its own extracted system, per the Ecosystem doc's §1.2. Grounded in the Group's live trade finance business (self-liquidating Bill for Collection, buyer-side aval confirmation as a hard fulfilment gate, revolving facility headroom).
- `docs/Sales_Order_Processing_DDD_Design.md` — the SOP counterpart to the Purchase Order Processing design doc: aggregates (`Customer`, `SalesOrder`, `AvalConfirmation`, `BillForCollection`, `FacilityHeadroom`), GL Engine crossing points (*Record sale*/*Record collection*). Everything it scopes is now built end to end, 2026-08-21: `SOP/` (106 tests) has the full domain layer plus its first application/infrastructure layers (`RecordSaleUseCase`/`RecordCollectionUseCase` calling out over HTTP), and `GL/` has real `RecordSaleUseCase`/`RecordCollectionUseCase` HTTP routes. Revenue-recognition timing resolved to the CIF loading point (candidate, pending contract confirmation); ECL destination resolved to stay GL-Engine-native (not yet reconnected — see the Sales Processing spec below).
- `docs/Sales_Processing_Requirements_Specification.md` — reference, 2026-08-21. A broader IFRS 15 (full 5-step)/IAS 2/IFRS 9/IAS 21/IAS 1/IFRS 13 spec, confirmed as a layer alongside SOP (not a replacement) — SOP's trade-finance scenario is one specific case of this more general model. IAS 21 FX chosen as the first build target (zero existing FX code anywhere); ECL reconnection and reconciling the full 5-step decomposition against SOP's simpler `SalesOrder` model are the next genuinely-open items.
- `POP/docs/Purchase_Order_Processing_Requirements_Use_Cases.md` — requirements/use-cases (UC-PO1-7/FR-PO01-08/NFR-PO01-05) for Purchase Order Processing as its own extracted system, per the Ecosystem doc's §1.1 and the detailed `docs/Purchase_Order_Processing_DDD_Design.md` follow-on. Reference-only, nothing built in `POP/` yet.
- `IM/docs/Inventory_Management_Requirements_Use_Cases.md` — requirements/use-cases (UC-IM1-7/FR-IM01-08/NFR-IM01-05) for Inventory Management as its own extracted system, per the Ecosystem doc's §1.3. Includes a cross-reference of every use case against the currently-built GL Engine code. **The §1.3 Option A/B fork is resolved as Option B, 2026-08-21**: `StockItem`'s whole costing engine moves out of the GL Engine into `IM/` rather than staying there — the GL Engine's role narrows to recording a caller-supplied *committed cost*, not computing one; costing is a business rule, not an accounting fact. `IM/` now has a real domain layer (95 tests): the physical/operational aggregates (`Item` master data, `GoodsReceiptConfirmation`/`GoodsIssueConfirmation` independent confirmation, `StockCountReconciliation`, `InventoryAdjustment`, `InTransitShipment`/`LocationState`, `EconomicOrderQuantity`/`ReorderPoint`/`ReplenishmentPolicy`) plus the ported IAS 2 costing engine itself (`Item`'s weighted-average/WIP/NRV state, `OverheadAllocation`). `GL/` got its first real GL-side migration increment the same day too: `RecordInventoryReceiptUseCase`/`RecordInventoryIssueUseCase` (563 GL tests) — added *alongside* the old `StockItem`-driven `PostInventoryReceiptUseCase`/`PostInventoryIssueUseCase`, not a replacement, mirroring `RecordSaleUseCase`/`RecordVendorObligationUseCase`'s established thin-interface shape. `StockItem`/`OverheadAllocation` are still fully present in `GL/` (`PurchaseOrder.send()`/`SalesOrder.deliverLine()` still depend on them) — see `docs/Ecosystem_Extraction_DDD_Design.md` §1.3's "concrete implications" for exact status.
- `docs/IFRS_GL_Posting_Matrix.md` — reference, 2026-08-21. A comprehensive IFRS posting matrix (Purchases/Sales/Inventory, IAS 2/IFRS 15/IFRS 9/IAS 21/IAS 1/IFRS 13) supplied directly in response to the Ecosystem doc's §2 atomicity question — reframes it: Goods-in-Transit/GRNI mean the AP and Inventory postings are usually genuinely sequential, not one atomic fact needing a combined endpoint, except under FOB Shipping Point control-transfer terms. **Goods-in-Transit/GRNI closed the same day** — the GL-Engine posting mechanics were already sufficient (`RecordVendorObligationUseCase`/`RecordInventoryReceiptUseCase` are fully generic); the real gap was `PurchaseOrder` never modeling delivery-terms/timing at all, fixed by adding `DeliveryTerms`, two new `PurchaseOrderStatus` values, and `receiveGoods()`/`receiveGoodsBeforeInvoice()`/`matchInvoice()` to `GL/`'s legacy `PurchaseOrder`, plus a real Flyway migration (`V7__purchase_order_delivery_terms.sql`). FIFO/Specific Identification, FX effects, and the IFRS 15 contract-asset/over-time/non-cash/principal-vs-agent scenarios remain unbuilt — see the doc's own "status against what's built" section for the full picture.
- `docs/GL_Production_Readiness_Assessment.md` — reference, 2026-08-21, updated 2026-09-03. A findings-only production-readiness audit of `GL/` originally (`POP`/`SOP`/`IM` have since gone from domain-layer-only to actually deployed alongside `HR`, so several findings now apply Platform-wide, not just to `GL/`). **Two findings resolved**: exception messages no longer leak to API clients via `StatusPages` (`GL/` only, 2026-08-21), and structured JSON logging + per-request `requestId` via Ktor's `CallId` plugin — resolved for `GL/` 2026-09-03, then ported the same day to `POP`/`SOP`/`IM`/`HR` (all five now emit JSON logs with a correlation id). Remaining open: no CD past "the image builds," no database-level tenant isolation/RLS, no metrics/tracing, a shallow `/health` check, no rate limiting, minimal HikariCP tuning, no dependency-vulnerability scanning, no documented backup/DR story, no graceful shutdown, no API versioning. Still a findings record, not a build plan — no severity-to-timeline mapping.
- `docs/POP_GL_Service_Account_Closure_Plan.md` — **CLOSED, live in production 2026-09-03.** POP's GL-calling credential (`POP_GL_ENGINE_BEARER_TOKEN`, previously a bare never-provisioned `System.getenv()` read causing every `/purchase-orders/{id}/match`/`/pay` call to throw and 500) now uses the same proven `CognitoServiceAccountTokenProvider` pattern as POP→IM and SOP/IM/HR→GL — new `pop_gl_service_account.tf`, a fourth `FISH_JWT_SERVICE_AUTH_NAME_POP` provider in `Auth.kt`, POP code shipped and deployed (image `b0fd8df`). Verified end to end via a real Cognito login + a real authenticated call to production GL's read-only posting-context endpoint (200, correctly shaped) — confirms the Cognito app client, GL's auth provider, and the `User`/`Membership` row all actually work together, not just that they're deployed. One residual, unrelated finding from that same verification call — closed same day: this production Company had no `2300` Trade Finance Facility Payable account configured; created it via the real API (`201`, account id `3a77498e-...`) and confirmed the posting-context endpoint now resolves it instead of returning `null`. One still-open meta-item: how the `User`/`Membership` row was actually provisioned remains undocumented, same gap SOP's/IM's own rows already had.
- `docs/Tenancy_Administration_Extraction_DDD_Design.md` — scoped `Tenant`/`User`/`Membership`/`Role`/`AccessLevel`/`ManagedModule`/`ModuleManagementPreference` moving out of `GL/` into **"Enterprise Administration" (EA)** — repo `fish-enterprise-administration`, folder `EA`, package `com.theprodeogroup.ea`. `Company` stays in GL (load-bearing for Ledger scoping). **Built and deployed 2026-09-05**: persistence/application/web/auth complete (57 unit + 6 integration tests), live in production at `ea-api.theprodeogroup.com` — real `infra/terraform/ea.tf`, own RDS database, ECS/ALB/ACM all applied. `Tenant.activate()` requires a Company (a real precondition found mid-build), so EA's own onboarding leaves a Tenant in `Draft`; `RegisterCompanyUseCase` closes the loop once a Company id is reported back. Not yet done: any GL/POP/SOP/IM/HR rewiring to actually call EA, migrating GL's live production Tenant/User/Membership rows, a CI pipeline for EA. **Scope expansion, same day**: the user supplied a full Business Owner Mobile ERP requirements/use-case doc (`EA/docs/Business_Owner_Mobile_ERP_Requirements_Use_Cases.md`) and confirmed it **is** EA's real intended charter, not a separate app — spanning Sales/Inventory/Purchasing/Finance/HR/Fixed-Assets/Communication, categorically larger than the Tenant/Administration slice actually built so far. Whether EA becomes an aggregation/orchestration layer over GL/POP/SOP/IM/HR, absorbs logic directly, or some mix is undecided — read that doc before any further EA design or build work.
- `FiSH_GL_Comparative_Analysis.docx` — how the SL business plan and the Engine Spec relate (market-first vs. architecture-first).
- `docs/FiSH_Women_SME_Problem_Case.docx` — Prodeo Capital working draft, August 2026. The problem case for why women-owned SMEs in the Mano River region can't get formal credit (no collateral, no records, informality, product mismatch) and where FiSH fits: not lending, but turning ledger activity into a lender-legible "repayment picture," positioned against real funding mechanisms (AFAWA, We-Fi, MUNAFA, gender bonds, 2X-aligned funds). **`docs/Women_SME_Support_Build_Brief.md` (2026-08-31)** translates it into an actual build brief, checked against real code state: the tenant dashboard's money-velocity KPI (`MoneyVelocityRoutes.kt`/`MeRoutes.kt`, shipped 2026-08-29) is confirmed as the seed of the "repayment picture," Osusu is flagged as the existing product already aimed at the savings-group behavior the source doc describes. Four blocking gaps flagged, not solved: the SLE-only currency decision excludes 3 of the source doc's 4 named countries, no basic-phone (USSD/SMS) channel exists, no consent/external-sharing mechanism exists for a tenant to grant a partner bank read access, and no paying tenants exist yet. Scopes one concrete first build (a consent-gated funder-ready export endpoint) with its field-shape and delivery-mechanism questions explicitly parked, not guessed. Design-only — nothing built yet.
- `docs/External_Regulatory_And_Banking_API_Integrations_Requirements_Specification.md` (2026-09-13) — requirements-only, grounded in real, dated research rather than assumed API shapes. Scopes three categories (Banking, Tax Authority, Company Registration) across the five jurisdictions already named elsewhere in this project (SL, UK, RoI, Liberia, Nigeria). Headline finding: **UK and Ireland are the only two jurisdictions with a genuine, documented API in all three categories** (Companies House + HMRC MTD; CRO + ROS, both PSD2-banking via aggregators like TrueLayer). Nigeria has a narrow e-invoicing API (FIRSMBS) and a CBN Open Banking framework whose national rollout is confirmed still incomplete as of its own Feb 2026 report — no company-registry or general tax-filing API. **Re-checked 2026-09-13**: materially further behind than "incomplete" suggested — CBN's own report shows Phase 1 of 3 (issuing its implementation roadmap) hadn't happened yet, no bank or aggregator confirmed live on the regulated standard, and whether a foreign entity can even register is unconfirmed either way — now moot for FiSH's own entry regardless, since a local Nigerian entity will be established per `docs/FiSH_Localization_Principle.md`, sidestepping the question rather than waiting on it. Sierra Leone and Liberia have **no government API in any of the three categories** — mobile money (Orange Money, MTN MoMo Open API) is the only genuinely accessible digital channel in either market, and Sierra Leone's own company-registrar authority is unsettled across sources (CAC-SL vs. NIB vs. OARG), flagged as an open question rather than picked. Nothing built — this is scoping only, including an open question on whether integrations belong in GL directly or as their own bounded context, mirroring the SOP/POP/IM/HR/EA extraction precedent. **`docs/IE/IE_MVP_Definition.md` (2026-09-13)** answers that open question for a first MVP (build inside GL, not a new service, until load justifies extraction) and cross-tabs two dimensions against Republic of Ireland specifically, on direct instruction: what "go-live ready" means for the platform itself for an RoI Company (Core Ledger/GL reporting/Corporate Income Tax's `RateStructure.CategorySplit` already model IE's 12.5%/25% split correctly — confirmed by reading the code, not assumed; VAT is a platform-wide gap, not RoI-specific, since no `TaxType.VAT` exists anywhere yet), and the minimum external-integration slice (CRO read-only lookup first as the lowest-risk proof of the integration shape, then a TrueLayer bank feed, then ROS VAT3 filing gated on the VAT `TaxType` question). RoI is deliberately the harder of the two mature jurisdictions (ROS's certificate auth vs. HMRC's OAuth; no single Irish banking standard vs. the UK's one) — the bet is that solving the harder case first makes the UK follow-on faster, not that RoI is the easier on-ramp. **Updated 2026-09-13 with two more go-live gates surfaced by `docs/FiSH_Localization_Principle.md`, neither a code gap**: the Irish subsidiary (decided, name pending between "Prodeo Capital IE"/"Prodeo EIRE") and the `eu-west-2` (London)-vs-`eu-west-1` (Ireland) data-residency question — both flagged as running parallel to, not gating, the engineering sequence, but needing to land before or alongside the first paying Irish customer, not after. **`docs/IE/IE_VAT_MVP_Design.md` (2026-09-13, supersedes that doc's earlier build order) reprioritizes VAT from a deferred gap to *the* MVP threshold**, on direct instruction, scoped to RoI only. Confirmed directly against code, not assumed: `TaxType` has only `CORPORATE_INCOME_TAX`; `ComputeTaxUseCase` is period-profit-based (wrong shape for VAT, which is transactional output-minus-input); no VAT field exists anywhere in SOP's sales lines or POP's purchase lines; no VAT control account exists in the Chart of Accounts template. Real Irish rates already researched (23%/13.5%/9% from 1 Jul 2026/4.8%/0%, five bands vs. UK's three). **All five open questions worked through to real decisions the same day**, on instruction: VAT category lives on the transaction line (`vatCategory` field), not IM's `Item` master — checked directly first, and `SalesOrderLine` turns out to have no `itemId` at all while `PurchaseOrderLine`'s is nullable and often absent, so an Item-sourced category wouldn't actually work against real data; a single netting "VAT Control Account" (liability-classified, balance directly is the return figure), not separate Payable/Receivable; bi-monthly filing cadence (Ireland's default) for MVP, other cadences deferred; VAT computation alone crosses the MVP bar, matching the precedent `ComputeTaxUseCase` already set for Corporate Income Tax — ROS filing (UC-EXT04) is the immediate fast-follow, not bundled in; and minimal effective-dating (an `asOf`/`effectiveFrom` lookup) built now rather than deferred, since the 9% rate change on 1 July 2026 is already legislated and inside this build's normal horizon.
- `docs/FiSH_Localization_Principle.md` (2026-09-13) — principle/design-only. FiSH shall be locally present in every jurisdiction it operates in, **as far as it is possible** — three genuinely different pillars (legal presence, data residency, product/regulatory localization), each triggered per-jurisdiction by actual need, not applied uniformly. Confirmed directly, not assumed: all six live services run from a single shared AWS region (`eu-west-2`, London), zero data-residency segregation by jurisdiction today. Decided the same day: an Irish subsidiary will be created to serve Irish customers (name pending between "Prodeo Capital IE"/"Prodeo EIRE"), and a Nigerian entity will likewise be stood up when FiSH enters that market. **Corrected same day** (see below) — an earlier overclaim that Nigeria's AWS-region gap made in-country hosting unsolved was wrong. **Sierra Leone/Liberia data residency researched same day, no longer an open flag**: Sierra Leone has no data protection law in force at all yet (a bill is pending, not enacted); Liberia's Data Protection Act was actually signed into law June 2026, but its central bank's own hosting rule (read directly) is a soft "should be local" preference with an offshore-permitted-if-CBL-has-access escape valve, not a hard mandate. Neither country currently forces a strict residency answer, and neither has real hyperscaler/colocation infrastructure maturity regardless.
- `docs/Nigeria_Compliant_Hosting_Opportunity.md` (2026-09-13) — vision-only, explicitly gated. Corrects an overclaim made the same day (in the Localization Principle doc and a board report draft) that Nigeria's 2027 CBN data-localization mandate was blocked by an unsolved infrastructure gap. It isn't: Huawei Cloud already operates a live, customer-provisionable region physically inside Nigeria (explicitly marketed for CBN/NDPC compliance, already used commercially), and Lagos already has mature Tier III colocation capacity (Rack Centre, MDXi, Africa Data Centres) — confirmed directly, not assumed; Azure/GCP/Oracle genuinely have no Nigerian region either, so this isn't uniquely an AWS gap. Reframes this as a genuine early-mover **business opportunity** — most competitors are likely on clouds that don't solve this. **Decided, same day, as an explicit two-phase plan**: Phase 1 — Huawei Cloud, **re-opened and confirmed against AWS's own Lagos Local Zone before locking in**, not assumed. The Local Zone lost on two confirmed specifics, not vibes: ECS on Fargate (what every FiSH service runs on) is explicitly unsupported there, and its control-plane depends on its parent region — `af-south-1`, Cape Town, not Nigeria — leaving the in-country compliance case genuinely unresolved. Huawei Cloud doesn't carry that same structural half-measure. A separate idea — Prodeo Group as "the portal through which AWS reaches Nigeria" — was checked and doesn't hold up (Outposts isn't resellable to third parties, Local Zones are AWS-only, and the real precedent, AWS Wavelength+Orange, requires telecom/carrier status Prodeo Group doesn't have); the AWS Partner Network (MSP track) is a real, open adjacent option instead — a services business on top of existing infrastructure, not a channel-partner one. Phase 2 — conditional, not a foregone conclusion: **if a real cost-vs-capital financial analysis (not yet done) shows the numbers support it, Prodeo Group invests in owning its own Nigerian infrastructure** rather than staying a Huawei Cloud customer indefinitely. Two real, unresolved shapes still flagged rather than picked: internal-only (just get FiSH's own Nigerian hosting compliant) vs. productized (sell this compliance expertise/hosting to other Nigerian financial institutions facing the same deadline, a materially bigger commitment closer to an infrastructure business). Explicitly gated behind Nigeria's own market-entry timeline, with one flagged nuance: this specific hosting decision may not need to wait for Nigeria's Open Banking rollout to mature, unlike the rest of Nigeria market entry.
- `docs/Inter_Tenant_Trade_Automation_Vision.md` (2026-09-13) — vision-only, explicitly gated, nothing to build yet. Pins down what "FiSH as a business-systems integrator" actually means, after pushback surfaced that the phrase reads as an Accenture/Deloitte-style consulting business (a different model entirely — services-led, headcount-scaled) rather than the user's real intent: a **Purchase Order raised by one FiSH tenant automatically becoming a Sales Order for another FiSH tenant**, a genuine network effect distinct from a generic ledger product. Confirmed real architecture gaps before scoping, not assumed: POP's `Supplier`/SOP's `Customer` are single-tenant concepts with no cross-tenant identity today, every existing service-to-service call is intra-tenant only, and the two line-item aggregates don't even share a shape (`SalesOrderLine` has no item reference at all). Explicit gating condition: revisit only once at least two real, paying tenants exist who actually want to trade with each other — until then this stays documented, not queued, so it doesn't compete with the RoI/VAT MVP work for the same thin team's time.
- `docs/FiSH_Localization_Principle.md` (2026-09-13) — a combined, best-effort ("as far as it is possible") principle covering legal presence, data residency, and product/regulatory localization, each triggered by what a given jurisdiction actually requires, not applied uniformly. Checked directly, not assumed: **all six live services run from a single shared AWS region, `eu-west-2` (London, UK)** — zero data-residency segregation by jurisdiction today, and directly relevant to the active RoI MVP work, since post-Brexit the UK is a GDPR "third country" relative to Ireland/the EU. Names a real, dated external constraint too: Nigeria has a confirmed 2027 CBN data-localization deadline (surfaced while researching the banking-rollout question), while AWS itself has no region inside Nigeria, Sierra Leone, or Liberia (`af-south-1`/Cape Town is its only African region) — true in-country hosting for those markets isn't achievable through AWS alone, flagged as an open constraint rather than glossed over. **Legal presence confirmed and decided same day**: Prodeo Group's current structure is a single UK Limited company, no local incorporation anywhere else — a real, concrete gap for the *active* RoI MVP, not a future hypothetical. Resolved by direct decision: **an Irish subsidiary of the Group will be created specifically to serve Irish customers** — the decision and legal form (subsidiary, not a branch of the UK entity) are both made; name still open between two candidates, "Prodeo Capital IE" or "Prodeo EIRE." Timing and operational mechanics (which entity holds the customer contract, how VAT filing on the customer's behalf works given a subsidiary is its own legal person) still need real Irish counsel input. **Extended to Nigeria the same day**: a local Nigerian entity will likewise be institutionalised whenever FiSH actually enters that market, deliberately sidestepping (not answering) the unresolved question of whether a foreign-incorporated company could register under Nigeria's Open Banking framework at all. Sierra Leone/Liberia haven't had this question raised yet — genuinely open, not decided by extension. Product localization assessed as the pillar already furthest along, citing the existing per-jurisdiction tax/currency docs as evidence. Recommends applying all three pillars per-jurisdiction in the same order as the rest of the platform's own sequencing (Ireland first), not simultaneously across every named market at once.
- **A full external code review's findings, all closed out 2026-09-14 through 2026-09-17.** Eight findings total, each investigated against real code before fixing rather than assumed: a CRITICAL cross-tenant Announcement-acknowledgment leak (EA, no `tenantId` check) and three HIGH app-layer bugs (any ADMIN could invite an OWNER_ADMIN; the Business Owner dashboard skipped a company∈tenant check before proxying; staff-invite tokens and the platform operator's bearer token were both stored/shared in the clear) are fixed, tested, and deployed. The bigger item — "no membership-check API" (design §2.4) — turned out to be a full rollout, not a one-line fix: `EA`'s proven `GET /me`/`EaMembershipGateway` pattern was piloted on `IM` then generalized to `POP` and `SOP`, each gated by its own `{Service}MembershipAuthorizer`; along the way this surfaced and resolved a real architecture question — **module-to-module (service-account) calls permanently bypass the EA membership check by design**, since Prodeo shouldn't be in the business of gating any Tenant's own internal integrations — recorded in `docs/Service_Account_Identity_And_EA_Membership_Design_Note.md`, with GL itself adopting the same bypass for its own outbound service calls. The EA_OPERATOR_TOKEN→EA_OPERATOR_TOKENS (named, per-operator) cutover surfaced a real deployment gotcha worth remembering for any future EA/POP/SOP/IM/HR terraform change to a live service's secrets or env vars: **Jenkins' own deploy stage only ever patches `.image` on whatever task definition is already live — it never re-reads `container_definitions` from terraform** — so a bare `terraform apply` renaming a secret resource in place would have destroyed the secret the *running* task still depended on (a same-database-wide outage risk, not just the operator routes), and a later Jenkins auto-deploy of the corrected code without a matching manual task-definition push left the operator surface silently 503ing for about a day before it was caught and fixed. The last HIGH finding — "tenancy only at app layer, no RLS" — is deliberately **not** built: `docs/Database_Tenant_Isolation_RLS_Scope.md` found no RLS/partitioning or session-context mechanism exists anywhere, that the finding's real blast radius is `GL`/`EA`/`HR` only (`POP`/`SOP`/`IM` are already physically isolated by separate per-service databases and are single-tenant-per-deployment today), and recommends holding off building it now that the concrete app-layer bugs are closed, tracking it as deliberate future work instead. Every repo's `README.md` (`GL`, `EA`, `POP`, `SOP`, `IM`, `HR`, `WEB`) was also found stale — several claiming "nothing built yet" for systems that have been live in production for weeks — and rewritten against actual current code/deployment state, not memory.
- `docs/Opening_Figures_CSV_Upload_Requirements_Specification.md` (2026-09-15, draft) — requirements for bulk CSV upload of opening figures (stock, fixed assets, AR, AP, GL balances) at a company's anchor date, so onboarding an established business doesn't mean one-row-at-a-time entry for everything it already owns/owes/is owed. Confirms, by reading the code, that every one of those five domains is single-record-only today (`RecordOpeningBalanceUseCase`, `CreateFixedAssetUseCase`, IM's `CreateItemUseCase`, SOP/POP's progressive entry) — no bulk path anywhere. Hard rule carried over from `docs/DDD_Design.md` §9.8: AR/AP opening figures stay itemized through SOP/POP, never a lump control-account import. Six open questions were left for Femi/Finance — see the design doc below, which resolves all six. **`docs/Opening_Figures_CSV_Upload_DDD_Design.md` (2026-09-18)** is that design pass: a generic GL-balance CSV targeting an account AR/AP/Inventory/FA already owns the itemized detail for gets redirected to the existing Suspense Account (`3910`) rather than blocked or allowed through — reusing the same "not yet classified" mechanism `FixedAssetFundingMethod.AlreadyOwned` already established, so the balance sheet stays correct while forcing proper itemization later. One new use case needed (`RecordOpeningStockUseCase`, IM); fixed assets and GL balances need no new use case at all, just the existing single-record use cases called once per CSV row; AR/AP need two new thin wrapping use cases (SOP/POP) whose contra is Suspense rather than a live revenue/expense account. Confirmed build order: GL balances first (exercises the Suspense-redirect every other domain leans on), then Fixed Assets, then Stock, then AR/AP last (the only domains needing cross-service coordination). Design-only — nothing built yet.
- **BuzzMe design work (2026-09-19) has moved out of this repo** — see the note at the top of this file. `docs/BuzzMe_DDD_Design.md`, `docs/BuzzMe_DPID_DDD_Design.md`, and `docs/DPID_System_Requirements_Specification.md` briefly existed here before being relocated to `BuzzMe/docs/` the same day, once "do not conflate with FiSH" was made explicit. Not read-these-first material for this repo any more.

## Current code state (2026-08-11, updated same day)

`GL/` is now a working Gradle Kotlin project (`build.gradle.kts`, `settings.gradle.kts`, wrapper — Kotlin 2.2.0, Gradle 8.10, JDK 21). `com.theprodeogroup.fish.domain.common` has 14 files under `src/main/kotlin/...` — value objects and enums only (`AuditAction`, `ClientType`, `PostingStatus`, `PeriodStatus`, `PeriodType`, `JournalSource`, `TransactionSide`, `DimensionType`, `FieldChange`, `ExportFormat`, `OrderBy`, `PaginatedResult`, `RequestContext`, `ValidationResult`, `DomainEvent`). `field_change.kt` is fixed (was truncated mid-declaration).

The first aggregate now exists: `com.theprodeogroup.fish.domain.tenancy.Tenant`, built ahead of the Ledger aggregates (Money/Account/Period/JournalEntry are still unbuilt) per explicit user direction, with `TenantStatus`/`TenantSegment`/`VerificationStatus`/`TenantId`/`CompanyId`/`MembershipId`/domain events alongside it. `Tenant` tracks `kybStatus` (business) and `adminKycStatus` (the founding admin's personal check, required for AML) as two independent `VerificationStatus` fields. 29 tests pass (`gradle test`). See `docs/DDD_Design.md` Section 9 for the design and Section 9.6 for exactly what's built vs. not (no `OnboardTenantUseCase`/application layer yet, no `Company`/`User`/`Membership` aggregates, no persistence).

## Repo structure

This is eleven git repos, not one — `FiSH/` (this repo — docs, business plans, `CLAUDE.md`) plus ten submodules, all **live in production** except `common`/`WEB`'s own status noted below:

| Path | GitHub repo | Visibility | What it is | Tests |
|---|---|---|---|---|
| `GL/` | `fish-fish-gl-engine` | public | The GLaaS Ledger engine — system of record for every financial posting. | 645 |
| `SOP/` | `fish-sales-order-processing` | private | Sales Order Processing (trade finance: Bill for Collection, aval confirmation). | 258 |
| `POP/` | `fish-purchase-order-processing` | private | Purchase Order Processing (suppliers, fulfillment, three-way match). | 214 |
| `IM/` | `fish-inventory-management` | private | Inventory Management (costing engine, warehouse operations). | 243 |
| `HR/` | `fish-hr-payroll` | private | HR/Payroll (Staff Cost Analysis; Performance Review designed, not built). | 203 |
| `EA/` | `fish-enterprise-administration` | private | Enterprise Administration — Tenant/User/Membership/Role, the Business Owner dashboard/communication surface, and the `GET /me` membership check every sibling calls. | 176 |
| `WEB/` | `fish-gl-web` | private | The React/PWA frontend (`fish-gl-web`), talking to all of the above. | — |
| `common/` | `fish-common` | public | Shared `Money`/`ValidationResult` value types, consumed as source (no published artifact) by GL/SOP/POP/IM/HR — see `common/README.md`. | — |
| `Infrastructure/` | `fish-infrastructure` | private | The shared Terraform project provisioning every service above plus platform pieces (network, RDS, ECS cluster, Cognito, Jenkins) — relocated out of `GL/infra/terraform/` 2026-09-17, see `docs/Platform_Infrastructure_Extraction_Design.md`. One shared repo, deliberately not split per service. | — |
| `ER/Principal/SchoolAdmissions/` | `fish-school-admissions` | private | SchoolAdmissions, the backend for **The Principal** (school EMIS, Education division) — Kotlin/Ktor/Exposed, financial events post through FiSH's SOP, never a direct GL client. Wired as a submodule and got its first GitHub remote + Jenkins CI/CD 2026-09-19; **not yet safe for real school data** (no Postgres integration test coverage, ER identity still a stub). | 78 |

**`BuzzMe` is not part of this table** — it is a wholly separate Prodeo Group system (`C:\Users\femif\Claude\Projects\BuzzMe`, its own git repo, own docs; live at `github.com/prodeo-group-dev/buzzme`, private, as of 2026-09-20 — under the same GitHub org as this family for convenience, not because it's a FiSH venture), explicitly not a FiSH venture or submodule. See the note at the top of this file.

`SOP`/`POP`/`IM`/`HR`/`EA`/`WEB`/`Infrastructure`/`ER/Principal/SchoolAdmissions` are **private** — several name a real counterparty, live trade finance deal terms, real AWS account details, or are simply not meant for public visibility, unlike `GL`/`fish`/`common`, which are public.

Every submodule is pinned to a specific commit in `.gitmodules` — cloning `FiSH/` needs `git clone --recurse-submodules` (or `git submodule update --init` after a plain clone) to actually get the code, and after pulling changes made *inside* any submodule, its pointer in `FiSH/` needs its own commit to move forward (it doesn't update automatically). Don't assume editing files under a submodule and committing in `FiSH/` is enough — submodule commits happen in their own repo first, `FiSH/` only records which commit it's pinned to. Each sibling repo's own `README.md` (refreshed 2026-09-17) is the accurate, current source for what's built in it — this table is a map, not the detail.

A prior attempt (in a separate, now-treated-as-lost chat) proposed splitting into ~47 files across 9 packages and only got partway through before creating duplicates. **Don't repeat that** — group related small value objects/enums in one package (as `common` already does) and only split further when a cluster of related types genuinely grows large.

## Conventions to follow

- **DDD**: see `docs/DDD_Design.md` for bounded contexts (Ledger = core domain; Tenancy, Lending, Tax = supporting) and aggregate boundaries. Ledger stays generic — Purse-specific rules (APR ceilings, arrears workflow, common-bond onboarding) live in the Lending context and depend on Ledger, never the reverse.
- **TDD**: write the test first. Recommended build order for the core Ledger aggregates: `Money` → `Account` → `Period` → `JournalEntry` (each depends on the previous). Test specs for these are already written given/when/then in `docs/DDD_Design.md` Section 6.
- **Testing stack**: not yet finalized — the default recommendation on the table is JUnit5 + Kotest assertions + MockK.
- **State machines**: encode transition rules inside the value type itself (see `PostingStatus.canTransitionTo()`, `PeriodStatus.isValidTransition()`), not scattered across services.
- **Validation**: return `ValidationResult` (existing `success()/failure()/withWarnings()/combine()` pattern) rather than throwing for expected domain-rule violations.
- **Money**: always amount + currency together, never a bare number — this doesn't exist in the codebase yet and should be built before `JournalLine`.
- **IDs**: prefer typed ID wrappers (`@JvmInline value class`) over raw `UUID` once aggregates are built, so the compiler catches an `AccountId` passed where a `TenantId` is expected.

## Configuration Management

**Direct instruction, 2026-09-24**: Configuration Management — infrastructure-as-code (`Infrastructure/`), CI/CD pipeline config (every repo's `Jenkinsfile`), and cross-service application config/secrets wiring (env vars, Secrets Manager entries, task-definition wiring) — is owned by the "Configuration Manager CM" Claude session going forward, as one discipline. If you're a different session about to touch any of these: check `Infrastructure/COORDINATION.md` first (same row-claim protocol as `ER/Principal/SchoolAdmissions/COORDINATION.md`) and consider flagging CM-shaped work to that session rather than applying it solo — this is a request, not something enforceable across independent sessions, but two sessions hit real duplicate-work collisions on exactly this kind of change on 2026-09-24 (see `Infrastructure/docs/Jenkins_Operations_Runbook.md`'s own incident note) before this convention existed.

A separate, recurring environment quirk (not this convention's job to fix): plan-mode's local plan file has repeatedly collided between concurrent sessions working the same task on 2026-09-24 (content from one session's plan appearing in another's) — flagged to Femi as a real tooling issue, not something either session caused or can patch around.

## Known environment constraint

The Cowork sandbox this project has mostly been worked in has no Kotlin compiler, no Gradle, and blocks network access to Maven Central / Gradle services / JetBrains downloads. Actual compilation and test execution needs to happen in an environment with real tooling (local machine, or a Claude Code session) — designs and code written in Cowork sessions should be treated as unverified until run there.

**This machine (the `GL/` local checkout) is not that sandbox** — it has Kotlin, Gradle, and JDK 21 installed with working internet access, confirmed 2026-08-11 by actually running `gradle test`. Code built here (starting with `Tenant`) is real and verified, not a design-only artifact — don't apply the "treat as unverified" caveat to it.
